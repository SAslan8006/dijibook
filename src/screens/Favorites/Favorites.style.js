import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {},    
    deleteIcon: {
        fontSize: 16,
        color: '#ffffff',
        backgroundColor: '#ef4444',
        padding: 8,
        borderRadius: 100,
    },
});